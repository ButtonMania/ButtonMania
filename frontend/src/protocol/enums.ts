
export enum MessageType {
    Update = 0,
    Record = 1,
    Error = 2
}

export enum ButtonType {
    Peace = "peace",
    Love = "love",
    Fortune = "fortune",
    Prestige = "prestige"
}

export enum ButtonPhase {
    Idle = "idle",
    Push = "push",
    Hold = "hold",
    Release = "release"
}

export enum UserLocale {
    EN = "en",
    RU = "ru"
}