
export enum GameState {
    Update = 0,
    Record = 1,
    Error = 3
}

export enum ButtonType {
    NewYear = "newyear",
    Peace = "peace",
    Love = "love",
    Fortune = "fortune",
    Prestige = "prestige",
}

export enum ButtonPhase {
    Idle = "idle",
    Push = "push",
    Hold = "hold",
    Release = "release"
}

export enum UserLocale {
    EN = "en",
    RU = "ru"
}